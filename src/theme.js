import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      light: "#ffffff",
      main: "#d3d3d3",
      dark: "#B3B3B3",
    },
    secondary: {
      main: "#fda403",
      dark: "#cb8402",
    },
  },
});

export default theme;
